import { Outlet } from 'react-router-dom';

export function ReclutadorLayout(){
  // Aca se tendria que usar un layout para el reclutador, asi todas las
  // rutas del reclutador comparten la misma estructura
  return <Outlet/>
}
